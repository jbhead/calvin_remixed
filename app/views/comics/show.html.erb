<div class="booyah-box col-10 offset-1">
  <h1><%= @comic.panel_description %></h1><br />
  <%= simple_form_for @panels, url: 'comic_panels_path(@comic)' do |f| %>
    <%= f.input :description %>
    <%= f.submit "Upload a panel", class: 'btn btn-primary' %>
  <% end %>
</div>
